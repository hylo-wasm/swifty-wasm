ignore:
  - '!(Sources)/'
comment:
  require_changes: true

coverage:
  status:

    project:
      default:
        threshold: 0%

    patch:
      default:
        target: auto
        threshold: 0%
        base: auto
